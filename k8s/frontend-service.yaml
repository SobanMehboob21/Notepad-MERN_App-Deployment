apiVersion: v1
kind: Service
metadata:
  name: frontend
spec:
  type: ClusterIP   # gives you an external IP; use NodePort if testing on minikube
  selector:
    app: frontend
  ports:
    - port: 80
      targetPort: 5173